# Copy this file to .env for local development
# For production deployment, set these as real environment variables

# If backend is on a different origin (e.g., Railway, Render), set this.
# Leave empty when using the Vite proxy (local dev).
VITE_API_BASE_URL=
